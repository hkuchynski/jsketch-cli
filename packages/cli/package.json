{
  "name": "jsketch-cli",
  "version": "2.0.0",
  "description": "A CLI to launch an interactive development environment for writing and documenting code.",
  "publishConfig": {
    "access": "public"
  },
  "bin": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "start": "tsc --watch --preserveWatchOutput",
    "prepublishOnly": "esbuild src/index.ts --platform=node --outfile=dist/index.js --bundle --minify --define:process.env.NODE_ENV=\\\"production\\\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@jsketch-cli/local-client": "^2.0.0"
  },
  "devDependencies": {
    "@jsketch-cli/local-api": "^2.0.0",
    "@types/node": "^22.10.5",
    "commander": "^11.0.0",
    "esbuild": "0.8.26",
    "typescript": "^5.2.2"
  }
}
